name: Scrape Memes

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:
    inputs:
      platforms:
        description: 'Platforms to scrape (space-separated). Threads/IG need residential proxies.'
        required: false
        default: 'ptt dcard'

jobs:
  scrape:
    name: Scrape Memes
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: scraper/requirements.txt

      - name: Install Python dependencies
        working-directory: scraper
        run: pip install -r requirements.txt

      - name: Install Chromium for patchright/playwright
        working-directory: scraper
        run: |
          python -m patchright install chromium || \
          python -m playwright install chromium

      - name: Install Chromium system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 \
            libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 \
            libxfixes3 libxrandr2 libgbm1 libasound2t64

      # GH Actions runners' default DNS resolver gets NXDOMAIN for www.dcard.tw
      # (likely Dcard geo-blocking GH IP ranges at the authoritative DNS layer).
      # Cloudflare's anycast resolver bypasses it. Stop systemd-resolved so it
      # can't rewrite resolv.conf back, then write our own.
      #
      # Chromium's built-in AsyncDns resolver (and patchright's stealth fork)
      # ignores resolv.conf overrides AND the --host-resolver-rules launch flag.
      # The only resolver hook every code path honors is /etc/hosts. Pin Dcard's
      # IPv4 there so Chromium falls into a hardcoded mapping at the OS level.
      - name: Use Cloudflare DNS resolver + pin Dcard IP in /etc/hosts
        run: |
          sudo systemctl disable --now systemd-resolved 2>/dev/null || true
          sudo rm -f /etc/resolv.conf
          sudo sh -c 'printf "nameserver 1.1.1.1\nnameserver 1.0.0.1\nnameserver 8.8.8.8\n" > /etc/resolv.conf'
          echo "--- /etc/resolv.conf ---"
          cat /etc/resolv.conf

          sudo apt-get install -y -qq dnsutils
          DCARD_IPV4=$(dig +short A www.dcard.tw @1.1.1.1 | grep -E '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' | head -1)
          if [ -z "$DCARD_IPV4" ]; then
            echo "::error::could not resolve A record for www.dcard.tw via 1.1.1.1"
            exit 1
          fi
          echo "Resolved www.dcard.tw → $DCARD_IPV4 — pinning in /etc/hosts"
          echo "$DCARD_IPV4 www.dcard.tw" | sudo tee -a /etc/hosts

          echo "--- DNS sanity check ---"
          getent hosts www.dcard.tw
          getent hosts www.ptt.cc

      - name: Run scraper
        working-directory: scraper
        env:
          NEON_DATABASE_URL:     ${{ secrets.NEON_DATABASE_URL }}
          R2_ENDPOINT:           ${{ secrets.R2_ENDPOINT }}
          R2_ACCESS_KEY_ID:      ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY:  ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET:             ${{ secrets.R2_BUCKET }}
          R2_PUBLIC_URL:         ${{ secrets.R2_PUBLIC_URL }}
        run: |
          PLATFORMS="${{ github.event.inputs.platforms || 'ptt dcard' }}"
          python main.py --platforms $PLATFORMS

      - name: Upload scraper logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-${{ github.run_id }}
          path: scraper/*.log
          if-no-files-found: ignore
